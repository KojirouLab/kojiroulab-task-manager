# セットアップ手順(初めての方向け)

このアプリは **パスコードでログイン** します。各社員が自分のパスコードで入り、自分に関係するタスクだけが見えます。
「管理者」に指定した人だけは、全員のタスクを見られます(「全員のタスク」タブ)。
パスコードの確認・管理者の判定はSupabase(サーバー側)で行うので、URLを知っているだけでは他人のページは開けません。

必要な準備は「Supabase(データとログインの管理)」と「GitHub Pages(アプリの公開場所)」です。どちらも無料で使えます。

## 1. Supabaseプロジェクトを作る

1. https://supabase.com にアクセスし、GitHubアカウントなどでサインアップ
2. 「New project」から新規プロジェクトを作成(リージョンは Tokyo (ap-northeast-1) がおすすめ)
3. 左メニューの **SQL Editor** を開き、このフォルダの `supabase.sql` の中身を貼り付けて実行
4. 左メニューの **Project Settings > API** を開き、Project URL と anon public key をメモして、
   `storage.js` の一番上の `SUPABASE_URL` と `SUPABASE_ANON_KEY` に貼り付ける

## 2. ログインの設定(重要)

左メニュー **Authentication** で次を設定します。

- **Sign In / Providers > Email**: 「Confirm email」を **オフ**、
  **「Allow new users to sign up」(新規登録を許可) を オフ** にする。
  こうすると、社員として登録した人以外は誰もアカウントを作れません。

## 3. 管理者用の機能を入れる

SQL Editor で、このフォルダの `supabase_admin.sql` の中身も貼り付けて実行します(`supabase.sql` を実行したあと)。
これで、管理者がアプリの画面から社員を追加・変更できるようになります。

同じ要領で、`supabase_priority.sql` も実行します(タスクに重要度 A/B/C を付けるための列を追加します)。

同じ要領で、`supabase_notify.sql` も実行します(Discord通知の仕組み。次の「Discord通知の設定」も参照)。

## Googleカレンダー連携の設定

期限のあるタスクを、**担当者のカレンダー**(「【重要度】タスク名」)と**依頼者のカレンダー**(「【依頼中】タスク名(担当: ○○)」)に、期限の日の**終日の予定**として入れます。期限・担当の変更やキャンセルも反映され、完了すると頭に✅が付きます。アプリ → Google の**一方向**です(Google側で動かしても、アプリには戻りません)。予定は、各自のGoogleアカウントに作られる「社内タスク」カレンダー(アプリが作ったもの)にだけ書きます。

`supabase_google.sql` を実行してから、次を行います。

1. **Google Cloudの設定**: プロジェクトを作り、Google Calendar API を有効化。OAuth同意画面(ユーザーの種類=外部、スコープ `https://www.googleapis.com/auth/calendar.app.created`、**公開ステータスは「本番環境」**。「テスト」のままだと連携が7日で切れます)。OAuthクライアント(種類=ウェブアプリケーション)を作り、承認済みのリダイレクトURIに `https://<Supabaseのプロジェクトref>.supabase.co/functions/v1/task-google-callback` を登録。
2. **クライアントシークレットをSupabaseに入れる**: Edge Functions > Secrets で、名前 `TASKMGR_GOOGLE_CLIENT_SECRET`。
3. **クライアントIDをDBに登録する**(公開されても問題ない値):
   ```sql
   insert into google_settings (key, value) values ('client_id', 'xxxx.apps.googleusercontent.com')
   on conflict (key) do update set value = excluded.value;
   ```
4. **Edge Functionをデプロイする**: `task-google-sync` と `task-google-callback` を、どちらも `--no-verify-jwt` を付けてデプロイ(`supabase/functions/_shared/` を共用しています)。
5. **各自が連携する**: 自分のページの「Googleカレンダー」→「Googleカレンダーと連携する」で、自分のGoogleアカウントで許可します(初回は「確認されていないアプリ」の警告が出ます。「詳細」→「(アプリ名)に移動」で進みます)。連携した時点で、既存の未完了・期限ありのタスクも入ります。
   - 連携の解除も同じ画面からできます(「社内タスク」カレンダーごと削除されます)。
- 期限のないタスクは、期限を付けた時点でカレンダーに入ります。
- Googleカレンダー側で予定を手動で消した場合は、次にそのタスクを変更したときに作り直されます。

## Discord通知の設定

タスクの依頼・担当変更・確認済み・完了・コメント・期限(前日と期限切れ)を、関係する本人のDiscordへ**Botからのダイレクトメッセージ(DM)**で知らせます。操作した本人には送りません。

1. **Botを作る**: https://discord.com/developers/applications で「New Application」(名前: `タスク通知`)→ 左の「Bot」→「Reset Token」で表示されるトークンをコピー(一度しか表示されません)。
2. **Botをサーバーに入れる**: 左の「OAuth2」→「OAuth2 URL Generator」で Scopes の `bot` にチェック → 表示されたURLを開き、会社のDiscordサーバーを選んで認証。
3. **トークンをSupabaseに入れる**: Supabaseの Edge Functions > Secrets で、名前 `DISCORD_BOT_TOKEN`、値=1のトークン、で追加。
4. **Edge Functionをデプロイする**: `supabase/functions/task-notify/` を、`--no-verify-jwt` を付けてデプロイします(この呼び出し元にはSupabaseのJWTがないため)。
5. **各メンバーのDiscordユーザーIDを登録する**: Discordの「ユーザー設定 → 詳細設定」で「開発者モード」をオン → サーバーのメンバー一覧で本人を右クリック →「ユーザーIDをコピー」。管理者の「メンバー管理」で、本人の名前をタップして貼り付けます。「テスト通知を送る」で届くか確認できます。
6. **各メンバーのお願い**: そのサーバーのプライバシー設定で「サーバーメンバーからのダイレクトメッセージを許可する」をオンにしてもらいます(オフだとDMが届きません。その場合は、テスト通知の結果にその旨が出ます)。

- DiscordユーザーIDが未登録の人には、通知は送られません(エラーにもなりません)。
- 通知の成否は `notify_log` に記録されます(管理者だけが読めます)。
- 期限の通知は、毎朝8時(日本時間)に判定します。対象は「明日が期限」と「期限を過ぎた翌日(1回だけ)」で、完了済みのタスクは対象外です。

## 4. 最初の管理者を1人登録する(1回だけ)

最初の1人だけは、アプリの画面から作れないので、次の2つをSupabase側で行います。「slug」は、その人の半角英数字のID(例: `endo`。英小文字・数字・ハイフンのみ)です。

**(a) ログイン用ユーザーを作る**
Authentication > Users > **Add user > Create new user** で次を入力し、**「Auto Confirm User」にチェック** を入れて作成します。

- Email: `slug@task-manager.example.com`(`@` より前が slug と一致していることが重要です。実在しないアドレスで構いません)
  - `@` より後ろを変えたい場合は `storage.js` の `LOGIN_DOMAIN` も同じ値にします。
- Password: 仮のパスコード(8文字以上)

**(b) 管理者として登録する**
```sql
insert into employees (slug, name, is_admin, sort_order) values ('endo', '遠藤克', true, 1);
```

## 5. 社員を増やす・変更する(管理者の画面から)

管理者でログインし、**「メンバー管理」タブ**で行います。

- **追加**: 「＋ メンバーを追加する」で、名前・ログインID・仮のパスコード(自動で作られます)を入れて追加します。表示された仮のパスコードを本人に伝えてください(この画面を閉じると再表示できません)。本人は初回ログイン時に、自分のパスコードを決めます。
- **名前の変更・管理者の切り替え・利用停止**: 名前をタップして変更します。利用停止にした人はログインできなくなり、担当者の選択肢からも消えます(過去のタスクの記録は残ります)。
- **パスコードを忘れた人**: 名前をタップして「パスコードのリセット」で、新しい仮のパスコードを設定します。本人は次回ログイン時にまた自分で決め直します。
- ご自身の管理者権限や利用状態は変更できません(管理者が誰もいなくなるのを防ぐため)。

## 6. GitHubで公開する

1. GitHubで新しいリポジトリを作成
2. この `task-manager` フォルダの中身をそのリポジトリにpush
3. リポジトリの **Settings > Pages** で、公開元を「main ブランチ / ルート」に設定
4. 数分待つと `https://ユーザー名.github.io/リポジトリ名/` でアプリが開けるようになります

## 7. 各メンバーに専用URLを案内する

社員一人ひとりに**専用のURL**があります(例: `https://…/kojiroulab-task-manager/?u=shoji`)。
管理者の「メンバー管理」で、名前をタップすると専用URLが表示されます(「URLをコピー」で共有できます)。メンバーを追加した直後の画面にも表示されます。

各自には、次のように案内してください。
1. 自分の専用URLを開く(「○○さん専用ページ」と表示されます)。
2. 仮のパスコードを入力してログインし、自分のパスコードを決める。
3. そのページをブックマーク、またはホーム画面に追加(iPhoneなら Safari の共有ボタン→「ホーム画面に追加」)。次回から、自分のページがすぐ開きます。

- URLだけでは入れません。必ずパスコードが必要です。専用URLは、名前が事前に決まっていて、パスコードだけを入れれば済む、というだけのものです。
- 他の人の専用URLを開くと、「このURLは○○さん専用です」と出ます(ログアウトして切り替えられます)。
- URLの `?u=` を付けずに開いた場合は、名前を選んでログインする画面になります。ログインすると、その人の専用URLに切り替わります。

## 8. 使い方(タスクのやり取りの流れ)

- 「＋ タスクを依頼する」から、タスク内容・担当者・**重要度(A高/B中/C低)**・期限・メモを入力して誰にでも依頼できます(自分自身に依頼することも可能です)。
- 依頼されたタスクは相手の「受けたタスク」に表示され、ステータスは次の3段階で進みます。
  1. **未確認**(依頼した直後)
  2. **確認済み**(担当者が「確認する」ボタンを押した状態。依頼した人には一覧のバッジで伝わります)
  3. **完了**(担当者が「完了にする」ボタンを押した状態)
- 担当者はタスク詳細から質問・コメントを送れます。依頼した人もタスク詳細から返信でき、やり取りは時系列のログとして残ります。
- 依頼した人は、タスクの内容(タスク内容・担当者・期限・メモ)の編集や、キャンセル(削除)ができます。
- **管理者**は「全員のタスク」タブで全員分の依頼・進捗を確認できます(人で絞り込み可)。他人のタスクは閲覧とコメントのみで、内容やステータスは変えられません。
- 自分のページでは、**上に「受けたタスク」、下に「依頼したタスク」**が並び、それぞれ**重要度A・B・Cが横の3列**になります(スマホなど画面が狭いときは縦に並びます)。各列は、期限が近いものが上に表示されます。完了したタスクは、「すべて」を選んだときだけ、最後にまとめて出ます。
- 重要度は、依頼者がタスクの詳細から後で変更できます。
- 期限を過ぎている未完了タスクは、一覧で赤字表示されます。
- 通知(LINEやメールなど)は届きません。相手の確認・完了状況は、アプリを開いて確認する運用です。

## 困ったときは

- 保存や読み込みに失敗する: 画面のエラーメッセージを確認し、通信状況を確認して再度お試しください。
  それでも直らない場合は `storage.js` の `SUPABASE_URL` / `SUPABASE_ANON_KEY` が正しいか確認してください。
- ログインできない: 名前・パスコードを確認。それでもだめなら、Authentication > Users にその人のメール(`slug@…`)があり、
  `employees` テーブルにも同じ slug の行があるか確認してください(どちらか片方だけだと入れません)。
